name: flutter_bridge
description: A Flutter bridge SDK for communication between Flutter and WebView
version: 1.0.0

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^12.0.0
  shared_preferences: ^2.2.0
  i18n:
    path: ../i18n

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
