name: app_update
description: App update module for Trip Route Track app
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  package_info_plus: ^8.0.0
  path_provider: ^2.1.0
  permission_handler: ^11.3.1
  i18n:
    path: ../i18n
  flutter_bridge:
    path: ../flutter_bridge

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
